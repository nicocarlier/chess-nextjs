/** @type {import('next').NextConfig} */
const nextConfig = {
    // rules: [
    //     {
    //       test: /\.(png|svg|jpg|jpeg|gif|ogg|mp3|wav)$/i,
    //       type: 'asset/resource',
    //     },
    //  ]
};

module.exports = nextConfig;
